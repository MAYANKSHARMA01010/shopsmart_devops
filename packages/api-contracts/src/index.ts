/**
 * @shopsmart/types — shared type definitions
 *
 * Consumed by server and client workspaces via workspace:* protocol.
 * Import from this package, not from individual files:
 *
 * @example
 *   import type { IPaymentProvider, ApiResponse } from '@shopsmart/types';
 */
export * from './api.types';
export * from './payment.types';
